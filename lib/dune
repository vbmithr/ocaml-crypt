(library
 (public_name crypt)
 (foreign_stubs
  (language c)
  (names crypt_stubs))
 (c_library_flags
  (:include c_library_flags.sexp)))

(rule
 (targets c_library_flags.sexp)
 (action
  (run ./config/discover.exe)))
